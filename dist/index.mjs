#!/usr/bin/env bun
import{McpServer as e}from"@modelcontextprotocol/server";import{serveStdio as t}from"@modelcontextprotocol/server/stdio";import{request as n,singleton as r}from"@rheactor/rheactor-core";import i from"zod";async function a(e,t,r){let i=await n({url:`https://api.openai.com/v1/responses`,method:`POST`,headers:{"content-type":`application/json`,authorization:`Bearer ${process.env.OPENAI_API_KEY}`},body:{model:`gpt-5.6-luna`,instructions:[`You are a web search assistant.`,`Your only job is to provide accurate, up-to-date answers by searching the web.`,``,`**Rules:**`,``,`1. Always respond in English;`,`2. Always search before stating any fact. Never invent or rely on memory for factual, time-sensitive, or external information;`,`3. Build queries as the user's question rephrased and expanded with clarifying keywords. For complex questions, run multiple queries;`,`4. Use the user's location only when the answer depends on it;`,`5. When the search succeeds, pass the returned text through unchanged, keeping inline citations intact.`,`   Pass sources through unchanged; do not filter, reorder, or reformat them.`,``,`**Answer depth and structure:**`,``,`1. Always produce a comprehensive answer, no matter how simple the question is. Never settle for a short, direct, or minimal reply;`,`2. Structure every answer in Markdown: open with a brief overview, then branch the topic into its relevant subtopics, contexts, and nuances using headings, subheadings, and lists;`,`3. Cover the question's branches implicitly within the answer, without listing them as explicit questions;`,`4. The main body must be detailed, roughly 3 to 10+ paragraphs. Each subtopic stays more direct, roughly 1 to 3 paragraphs;`,`5. Keep the expansion to one level: the main answer plus its direct subtopics, without cascading into deeper nesting;`,`6. Include only branches that help fully understand the question; do not pad the answer with unrelated tangents.`].join(`
`),input:e,text:{verbosity:`high`},tools:[{type:`web_search`,user_location:t,search_context_size:r}],tool_choice:`required`,include:[`web_search_call.action.sources`]}});if(!i.data)throw Error(`No response data`);return i.data.output.find(e=>e.type===`message`).content.find(e=>e.type===`output_text`).text}const o=[`low`,`medium`,`high`];r(()=>t(()=>{let t=new e({name:`rheactor-search-mcp`,version:`1.0.0`});return t.registerTool(`web_search`,{inputSchema:i.object({query:i.string().min(1).describe(`Search query to execute. Build it by rephrasing and expanding the user's question with clarifying keywords for best results.`),userLocation:i.object({city:i.string().optional(),country:i.string().optional()}).optional().describe(`User's location, only when the answer depends on it. Ask the user for it when relevant and not yet known; otherwise infer it from context when possible.`),searchContextSize:i.enum(o).optional().describe(`Desired depth of the search and answer. low: quick summary for simple questions; medium: balanced answer (default); high: comprehensive answer with more sources for complex questions.`)}),description:[`Performs a web search to find current, factual information relevant to the user's question. Returns a synthesized answer with inline citations, the sources consulted, and the exact queries executed. Use this tool as the primary source for factual, time-sensitive, or external information.`,`You can also use it to find useful URLs related to the content and delve deeper into them using other tools.`,`Set timeout to 150s.`,`Output shape:`,JSON.stringify({text:`<answer with inline citations>`,sources:[{title:`<page title>`,url:`<page url>`}],queries:[`<search queries actually executed>`]})].join(`

`)},async({query:e,userLocation:t,searchContextSize:n})=>{try{let r=await a(e,t,n);return{content:[{type:`text`,text:JSON.stringify(r)}]}}catch(e){if(e instanceof Error)return{content:[{type:`text`,text:e.message}],isError:!0};throw e}}),t}))();export{};